version: 2
jobs:
  build:
    working_directory: ~/mern-starter
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run:
          command: pwd
      - run:
          command: ls -al
